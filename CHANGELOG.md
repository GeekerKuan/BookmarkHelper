# 版本更新日志

本文件区分“当前 Android 12+ 现代化测试分支”和“原 BookmarkHelper 已发布历史”。
原项目中标记为 Draft 的 0.0.24、0.1.x 计划不视为已经实现。

## 1.0.0-alpha.1

- 修复 Android 16 上 Edge `Default/tabs` 位于 `/data_mirror/data_ce` 时被安全路径白名单拒绝的问题；仍拒绝路径穿越和非应用私有目录。
- Edge 标签页异常会记录精确阶段和异常类型，不记录网址、标题或真实文件路径。
- 主页采用 SukiSU 风格的信息布局；Root 与 LSPosed 状态卡分别响应，新增 Root 管理器、应用版本、浏览器安装版本及导入/管理入口。
- 运行中或待确认任务改为只在数据导入页动态显示；完成后隐藏，待确认时显示红点并发送可直达确认页的系统通知。
- 任务新增协作式安全取消；排队和待确认任务立即停止，进行中的 Root 文件操作在原子边界后停止。
- 数据管理加入“全部来源 / Via 数据 / Edge 数据”逻辑目录，收藏支持创建浏览器专属文件夹、父子页面和批量移动。
- 数据管理改为长期资料库；清理旧任务快照不会再删除用户保存的收藏、历史记录或标签页。
- 移除可选择的 Material 3 主界面，旧设置值自动迁移到 Miuix。

## 2026-08 · 标签页路径适配

- Via 与 Edge 标签页读取补齐 Android 16 Root 环境的 `data_mirror/data_ce` 路径探测。
- 使用 Via 6.7.1 新样本确认 `tabs` 表结构、测试标签与毫秒时间戳兼容。
- 标签页任务新增隐私安全阶段码；诊断日志只记录浏览器、阶段、异常类型和数量。

## 2026-08 · 标签页与文件夹层级

- 新增 Via 与 Edge 当前已打开标签页读取；Edge 隐私、归档、内部页面和自定义标签不进入数据管理。
- 收藏文件夹改为真正的父子 Activity 页面，可逐级进入并用系统返回回到父文件夹。
- 历史范围滑块改为 1、7、30、90、180、365 天六个等距节点；新增精确日期范围子页面。
- “正在运行的任务”入口移到主页，应用信息移到主页末尾。
- 收藏确认按钮移动到网址预览之前，并使用 Miuix 主操作强调色。
- 收藏、历史和标签页编辑均可直接调用系统浏览器打开链接。

## 2026-08 · 多浏览器数据管理

- 主页新增 Root 与 LSPosed 运行状态卡，并仅为这两张卡启用 Miuix 3D 按压反馈。
- “数据迁移”重构为“数据导入”；收藏、历史记录和标签页分别进入浏览器选择页面。
- 收藏和历史记录可同时选择 Via 与 Edge；历史记录支持自由选择最近 1 至 365 天。
- 浏览器数据先解析为中立格式，再保存到应用私有数据管理，不再把 Edge 写入写死在导入界面。
- 数据管理按收藏文件夹、历史年月日分组，加入分组全选、批量移动和批量删除。
- 主题设置沿用 SukiSU-Ultra 的图标映射；页面缩放改为拖动时即时生效。

## 2026-08 · Miuix 与 Edge 读取

- 将预见式返回、系统触感、顶栏/底栏模糊接入真实运行时行为。
- 移除自定义 Activity 进出动画，使用 Android 与系统界面的原生过渡。
- 修复 Miuix 下拉菜单宿主；页面缩放在滑块结束后立即生效。
- Edge 收藏和历史记录接入浏览器中立适配器；一次读取同时完成判重和数据管理入库。
- 新增应用信息页面，并将版本记录、开源许可和开发者选项入口集中管理。
- 新增独立的版本更新日志子页面。

## 2026-08 · 数据管理与任务中心

- 建立应用私有 SQLite 数据仓库，分别管理收藏、历史记录和标签页。
- 加入来源标记、跨浏览器去重、分页搜索、编辑和删除。
- 将新建任务、运行进度和后台等待拆分为独立页面，冲突任务自动排队。
- 开发者选项改为连续点击版本 7 次开启；诊断包执行双层脱敏。

## 2026-08 · 历史记录与安全事务

- 加入 Via 历史时间范围读取和 Edge Chromium History 写入实验。
- Root 写入增加单飞锁、备份、元数据核验、持久事务日志和失败关闭。
- 建立浏览器中立的收藏、历史记录和标签页模型。

## 2026-08 · Android 12+ 现代化

- 最低系统提升至 Android 12，适配 Android 16/17、AndroidX、Java 17 和新版 Gradle。
- 实现 Root 环境下 Via → Edge 收藏预览、去重、备份和安全写入。
- 加入 Miuix/Material 双界面、悬浮底栏、主题设置和脱敏诊断导出。
- 加入 LSPosed 标签云同步可行性实验，但不承诺 Edge 账号云同步。

## 0.0.15–0.0.23（原项目）

- 陆续支持 Chrome Dev、MIUI、Yandex、旗鱼、H5、星尘、360 极速、一加 QQ 及多个 X 浏览器版本。
- 持续修复 UC、Flyme、星尘等数据路径和 SQLite 兼容问题。

## 0.0.6–0.0.14（原项目）

- 增加 360、搜狗、百度、欧朋、猎豹、Firefox、UC 国际版、Chrome Canary/Beta。
- 完善嵌套文件夹、文件夹路径、重复性验证、Root/Shell 稳定性和异步转换。

## 0.0.1–0.0.5（原项目）

- 0.0.1 首次支持从 Chrome 提取书签并追加到 Via。
- 随后加入 Flyme/通用系统浏览器、UC、QQ 和 X 浏览器。
- 开始进行 UI 重构、登录用户数据兼容、异步日志和非阻塞 Root 检查。

更细的原项目逐版本记录仍保留在 [README.md](README.md) 中。
